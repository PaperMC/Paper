--- a/net/minecraft/server/players/UserWhiteList.java
+++ b/net/minecraft/server/players/UserWhiteList.java
@@ -21,6 +_,11 @@
 
     @Override
     public boolean add(UserWhiteListEntry userWhiteListEntry) {
+        // Paper start - Add whitelist events
+        if (!new io.papermc.paper.event.server.WhitelistStateUpdateEvent(com.destroystokyo.paper.profile.CraftPlayerProfile.asBukkitCopy(userWhiteListEntry.getUser().toUncompletedGameProfile()), io.papermc.paper.event.server.WhitelistStateUpdateEvent.WhitelistStatus.ADDED).callEvent()) {
+            return false;
+        }
+        // Paper end - Add whitelist events
         if (super.add(userWhiteListEntry)) {
             if (userWhiteListEntry.getUser() != null) {
                 this.notificationService.playerAddedToAllowlist(userWhiteListEntry.getUser());
@@ -34,6 +_,11 @@
 
     @Override
     public boolean remove(NameAndId nameAndId) {
+        // Paper start - Add whitelist events
+        if (!new io.papermc.paper.event.server.WhitelistStateUpdateEvent(com.destroystokyo.paper.profile.CraftPlayerProfile.asBukkitCopy(nameAndId.toUncompletedGameProfile()), io.papermc.paper.event.server.WhitelistStateUpdateEvent.WhitelistStatus.REMOVED).callEvent()) {
+            return false;
+        }
+        // Paper end - Add whitelist events
         if (super.remove(nameAndId)) {
             this.notificationService.playerRemovedFromAllowlist(nameAndId);
             return true;
