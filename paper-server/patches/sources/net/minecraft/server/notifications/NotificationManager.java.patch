--- a/net/minecraft/server/notifications/NotificationManager.java
+++ b/net/minecraft/server/notifications/NotificationManager.java
@@ -87,8 +_,8 @@
     }
 
     @Override
-    public void onGameRuleChanged(String key, GameRules.Value<?> value) {
-        this.notificationServices.forEach(notificationService -> notificationService.onGameRuleChanged(key, value));
+    public void onGameRuleChanged(net.minecraft.server.level.ServerLevel level, String key, GameRules.Value<?> value) { // Paper - per-world game rules (add level param)
+        this.notificationServices.forEach(notificationService -> notificationService.onGameRuleChanged(level, key, value)); // Paper - per-world game rules (add level param)
     }
 
     @Override
