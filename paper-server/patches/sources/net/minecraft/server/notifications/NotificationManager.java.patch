--- a/net/minecraft/server/notifications/NotificationManager.java
+++ b/net/minecraft/server/notifications/NotificationManager.java
@@ -87,8 +_,8 @@
     }
 
     @Override
-    public void onGameRuleChanged(String string, GameRules.Value<?> value) {
-        this.notificationServices.forEach(notificationService -> notificationService.onGameRuleChanged(string, value));
+    public void onGameRuleChanged(net.minecraft.server.level.ServerLevel level, String string, GameRules.Value<?> value) { // Paper - per-world game rules (add level param)
+        this.notificationServices.forEach(notificationService -> notificationService.onGameRuleChanged(level, string, value)); // Paper - per-world game rules (add level param)
     }
 
     @Override
