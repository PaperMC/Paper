--- a/net/minecraft/server/commands/RandomCommand.java
+++ b/net/minecraft/server/commands/RandomCommand.java
@@ -42,7 +_,7 @@
                 .then(drawRandomValueTree("roll", true))
                 .then(
                     Commands.literal("reset")
-                        .requires(Commands.hasPermission(Commands.LEVEL_GAMEMASTERS))
+                        .requires(stack -> stack.getSender().hasPermission("minecraft.command.random.reset")) // Paper - Add permission node for reset
                         .then(
                             Commands.literal("*")
                                 .executes(c -> resetAllSequences(c.getSource()))
@@ -122,7 +_,7 @@
                     .then(
                         Commands.argument("sequence", IdentifierArgument.id())
                             .suggests(RandomCommand::suggestRandomSequence)
-                            .requires(Commands.hasPermission(Commands.LEVEL_GAMEMASTERS))
+                            .requires(stack -> stack.getSender().hasPermission("minecraft.command.random.sequence." + (announce ? "roll" : "value"))) // Paper - Split permission node for sequence
                             .executes(
                                 c -> randomSample(c.getSource(), RangeArgument.Ints.getRange(c, "range"), IdentifierArgument.getId(c, "sequence"), announce)
                             )
