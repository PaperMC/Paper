--- a/net/minecraft/server/packs/PathPackResources.java
+++ b/net/minecraft/server/packs/PathPackResources.java
@@ -134,7 +_,9 @@
         try (DirectoryStream<Path> directDirs = Files.newDirectoryStream(rootDir)) {
             for (Path directDir : directDirs) {
                 if (!Files.isDirectory(directDir)) {
-                    LOGGER.warn("Non-directory entry {} found in namespace directory, rejecting", directDir);
+                    // Paper start - Improve logging and errors
+                    LOGGER.error("Invalid directory entry: {} in {}.", directDir.getFileName().toString(), rootDir, new java.nio.file.NotDirectoryException(directDir.getFileName().toString()));
+                    // Paper end - Improve logging and errors
                 } else {
                     String namespace = directDir.getFileName().toString();
                     if (Identifier.isValidNamespace(namespace)) {
