--- a/net/minecraft/gametest/framework/GameTestHelper.java
+++ b/net/minecraft/gametest/framework/GameTestHelper.java
@@ -929,7 +_,7 @@
 
     public void setTime(final long ticks) {
         ServerLevel level = this.getLevel();
-        Holder<WorldClock> clock = level.dimensionType().defaultClock().orElseThrow();
+        Holder<WorldClock> clock = level.getClock().orElseThrow();
         level.clockManager().setTotalTicks(clock, ticks);
     }
 
