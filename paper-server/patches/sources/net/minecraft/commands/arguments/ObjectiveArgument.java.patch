--- a/net/minecraft/commands/arguments/ObjectiveArgument.java
+++ b/net/minecraft/commands/arguments/ObjectiveArgument.java
@@ -32,6 +_,12 @@
     public static Objective getObjective(final CommandContext<CommandSourceStack> context, final String name) throws CommandSyntaxException {
         String id = context.getArgument(name, String.class);
         Scoreboard scoreboard = context.getSource().getServer().getScoreboard();
+        // Paper start - expose objective argument
+        return getObjective(scoreboard, id);
+    }
+
+    public static Objective getObjective(final Scoreboard scoreboard, final String id) throws CommandSyntaxException {
+        // Paper end - expose objective argument
         Objective objective = scoreboard.getObjective(id);
         if (objective == null) {
             throw ERROR_OBJECTIVE_NOT_FOUND.create(id);
@@ -41,7 +_,14 @@
     }
 
     public static Objective getWritableObjective(final CommandContext<CommandSourceStack> context, final String name) throws CommandSyntaxException {
-        Objective objective = getObjective(context, name);
+        // Paper start - expose objective argument
+        String id = context.getArgument(name, String.class);
+        return getWritableObjective(context.getSource().getServer().getScoreboard(), id);
+    }
+
+    public static Objective getWritableObjective(final Scoreboard scoreboard, final String id) throws CommandSyntaxException {
+        Objective objective = getObjective(scoreboard, id);
+        // Paper end - expose objective argument
         if (objective.getCriteria().isReadOnly()) {
             throw ERROR_OBJECTIVE_READ_ONLY.create(objective.getName());
         } else {
