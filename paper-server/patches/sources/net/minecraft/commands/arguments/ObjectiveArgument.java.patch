--- a/net/minecraft/commands/arguments/ObjectiveArgument.java
+++ b/net/minecraft/commands/arguments/ObjectiveArgument.java
@@ -48,6 +_,25 @@
             return objective;
         }
     }
+    // Paper start - expose objective argument
+    public static Objective getObjective(Scoreboard scoreboard, String string) throws CommandSyntaxException {
+        Objective objective = scoreboard.getObjective(string);
+        if (objective == null) {
+            throw ERROR_OBJECTIVE_NOT_FOUND.create(string);
+        } else {
+            return objective;
+        }
+    }
+
+    public static Objective getWritableObjective(Scoreboard scoreboard, String string) throws CommandSyntaxException {
+        Objective objective = getObjective(scoreboard, string);
+        if (objective.getCriteria().isReadOnly()) {
+            throw ERROR_OBJECTIVE_READ_ONLY.create(objective.getName());
+        } else {
+            return objective;
+        }
+    }
+    // Paper end
 
     @Override
     public String parse(StringReader reader) throws CommandSyntaxException {
