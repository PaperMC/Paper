--- a/net/minecraft/world/level/SignalGetter.java
+++ b/net/minecraft/world/level/SignalGetter.java
@@ -93,4 +_,27 @@
 
         return i;
     }
+
+    // Paper start - Add getBestNeighborSignalPos for get the BlockPos of the best signal based in getBestNeighborSignal
+    @javax.annotation.Nullable
+    default BlockPos getBestNeighborSignalPos(BlockPos pos) {
+        BlockPos bestPos = null;
+        int i = 0;
+
+        for (Direction direction : DIRECTIONS) {
+            BlockPos signalPos = pos.relative(direction);
+            int signal = this.getSignal(signalPos, direction);
+            if (signal >= 15) {
+                return signalPos;
+            }
+
+            if (signal > i) {
+                bestPos = signalPos;
+                i = signal;
+            }
+        }
+
+        return bestPos;
+    }
+    // Paper end
 }
