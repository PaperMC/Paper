--- a/net/minecraft/world/level/gameevent/EntityPositionSource.java
+++ b/net/minecraft/world/level/gameevent/EntityPositionSource.java
@@ -51,6 +_,15 @@
 
         return this.entityOrUuidOrId.left().map(entity -> entity.position().add(0.0, this.yOffset, 0.0));
     }
+    // Paper start - make Entity directly retrievable
+    public Optional<Entity> getEntity(Level level) {
+        if (this.entityOrUuidOrId.left().isEmpty()) {
+            this.resolveEntity(level);
+        }
+
+        return this.entityOrUuidOrId.left();
+    }
+    // Paper end
 
     private void resolveEntity(Level level) {
         this.entityOrUuidOrId
