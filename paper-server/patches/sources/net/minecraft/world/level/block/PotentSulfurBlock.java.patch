--- a/net/minecraft/world/level/block/PotentSulfurBlock.java
+++ b/net/minecraft/world/level/block/PotentSulfurBlock.java
@@ -70,6 +_,13 @@
             return state.setValue(STATE, PotentSulfurState.DRY);
         }
 
+        // Paper start - eruption mode API
+        final BlockState eruptionModeState = org.bukkit.craftbukkit.block.CraftPotentSulfur.applyEruptionMode(state, level, pos);
+        if (eruptionModeState != null) {
+            return eruptionModeState;
+        }
+        // Paper end - eruption mode API
+
         BlockState belowState = level.getBlockState(pos.below());
         if (belowState.is(BlockTags.CAUSES_CONTINUOUS_GEYSER_ERUPTIONS) && isSourceIfFluid(belowState)) {
             return state.setValue(STATE, PotentSulfurState.CONTINUOUS);
