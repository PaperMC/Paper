--- a/net/minecraft/world/level/block/SlimeBlock.java
+++ b/net/minecraft/world/level/block/SlimeBlock.java
@@ -21,6 +_,9 @@
 
     @Override
     public void fallOn(final Level level, final BlockState state, final BlockPos pos, final Entity entity, final double fallDistance) {
+        if(!org.bukkit.craftbukkit.event.CraftEventFactory.callEntityLandEvent(state, level, pos, entity, false)){ // Paper start - Add EntityLandEvent
+            return;
+        } // Paper end - Add EntityLandEvent
         if (!entity.isSuppressingBounce()) {
             entity.causeFallDamage(fallDistance, 0.0F, level.damageSources().fall());
         }
