--- a/net/minecraft/world/level/storage/loot/LootContext.java
+++ b/net/minecraft/world/level/storage/loot/LootContext.java
@@ -142,6 +_,15 @@
             RandomSource random = Optional.ofNullable(this.random).or(() -> randomSequenceKey.map(server::getRandomSequence)).orElseGet(level::getRandom);
             return new LootContext(this.params, random, server.reloadableRegistries().lookup());
         }
+
+        // Paper start - bypass level for fake context
+        @Deprecated
+        public LootContext createWithoutLevel() {
+            MinecraftServer server = MinecraftServer.getServer();
+            RandomSource random = Optional.ofNullable(this.random).orElseThrow();
+            return new LootContext(this.params, random, server.reloadableRegistries().lookup());
+        }
+        // Paper end - bypass level for fake context
     }
 
     public enum EntityTarget implements StringRepresentable, LootContextArg.SimpleGetter<Entity> {
