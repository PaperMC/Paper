From 6c548a45b83ce7c45456f21c6ea770305304f9f3 Mon Sep 17 00:00:00 2001
From: MrMaurice211 <t_ulu_m@live.com.mx>
Date: Fri, 9 Aug 2019 20:20:24 -0500
Subject: [PATCH] New non-deprecated method to get MapView from id


diff --git a/src/main/java/org/bukkit/Bukkit.java b/src/main/java/org/bukkit/Bukkit.java
index 6db69159..693c2860 100644
--- a/src/main/java/org/bukkit/Bukkit.java
+++ b/src/main/java/org/bukkit/Bukkit.java
@@ -548,6 +548,20 @@ public final class Bukkit {
     public static MapView getMap(int id) {
         return server.getMap(id);
     }
+    
+    // Paper start - A way to get MapView without being deprecated
+    /**
+     * Gets the map from the given ID.
+     *
+     * @param world the world the map will belong to
+     * @param id the id of the map to get
+     * @return a map view if it exists, or null otherwise
+     */
+    @Nullable
+    public static MapView getMap(@NotNull World world, int id) {
+        return server.getMap(world, id);
+    }
+    // Paper end
 
     /**
      * Create a new map with an automatically assigned ID.
diff --git a/src/main/java/org/bukkit/Server.java b/src/main/java/org/bukkit/Server.java
index 27d3b5ad..269bc0c3 100644
--- a/src/main/java/org/bukkit/Server.java
+++ b/src/main/java/org/bukkit/Server.java
@@ -459,6 +459,18 @@ public interface Server extends PluginMessageRecipient {
     @Deprecated
     @Nullable
     public MapView getMap(int id);
+    
+	// Paper start - A way to get MapView without being deprecated
+    /**
+     * Gets the map from the given ID.
+     *
+     * @param world the world the map will belong to
+     * @param id the id of the map to get
+     * @return a map view if it exists, or null otherwise
+     */
+    @Nullable
+    public MapView getMap(@NotNull World world, int id);
+    // Paper end
 
     /**
      * Create a new map with an automatically assigned ID.
-- 
2.18.0.windows.1

