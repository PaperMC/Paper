From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: DavidTs93 <david.ts93@gmail.com>
Date: Fri, 7 Apr 2023 19:28:25 +0300
Subject: [PATCH] Add mob spawner ranges API


diff --git a/src/main/java/org/bukkit/block/CreatureSpawner.java b/src/main/java/org/bukkit/block/CreatureSpawner.java
index 3dcab76632c4f8646d7ae62bb8de287eb6ef55b5..c853c5af8b70eaceec099adb508aad6690995d82 100644
--- a/src/main/java/org/bukkit/block/CreatureSpawner.java
+++ b/src/main/java/org/bukkit/block/CreatureSpawner.java
@@ -176,31 +176,50 @@ public interface CreatureSpawner extends TileState {
     public void setRequiredPlayerRange(int requiredPlayerRange);
 
     /**
-     * Get the radius around which the spawner will attempt to spawn mobs in.
+     * Get the horizontal radius around which the spawner will attempt to spawn mobs in.
      * <br>
      * This area is square, includes the block the spawner is in, and is
      * centered on the spawner's x,z coordinates - not the spawner itself.
      * <br>
-     * It is 2 blocks high, centered on the spawner's y-coordinate (its bottom);
+     * It is {@link #getVerticalSpawnRange()} + 1 blocks high, centered on the spawner's y coordinate (its bottom);
      * thus allowing mobs to spawn as high as its top surface and as low
-     * as 1 block below its bottom surface.
+     * as {@link #getVerticalSpawnRange()} blocks below its bottom surface.
      * <br>
      * Default value is 4.
      *
-     * @return the spawn range
+     * @return the horizontal spawn range
      */
     public int getSpawnRange();
 
     /**
-     * Set the new spawn range.
+     * Set the new horizontal spawn range.
      * <br>
      *
-     * @param spawnRange the new spawn range
+     * @param spawnRange the new horizontal spawn range
      * @see #getSpawnRange()
      */
     public void setSpawnRange(int spawnRange);
 
     // Paper start
+    /**
+     * Get the vertical radius around which the spawner will attempt to spawn mobs in.
+     * <br>
+     * Default value is 1.
+     *
+     * @return the vertical spawn range
+     * @see #getSpawnRange()
+     */
+    public int getVerticalSpawnRange();
+    
+    /**
+     * Set the new vertical spawn range.
+     * <br>
+     *
+     * @param verticalSpawnRange the new vertical spawn range
+     * @see #getVerticalSpawnRange()
+     */
+    public void setVerticalSpawnRange(int verticalSpawnRange);
+
     /**
      * Check if spawner is activated (a player is close enough)
      *
