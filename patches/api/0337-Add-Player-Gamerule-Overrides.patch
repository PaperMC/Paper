From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Owen1212055 <23108066+Owen1212055@users.noreply.github.com>
Date: Wed, 6 Oct 2021 18:16:56 -0400
Subject: [PATCH] Add Player Gamerule Overrides


diff --git a/src/main/java/org/bukkit/entity/Player.java b/src/main/java/org/bukkit/entity/Player.java
index 3e8cd3971ac8256a40d9b85cd7514998c965512c..ade7f0adc1818d5fed045db84ddfd9b6df7cb479 100644
--- a/src/main/java/org/bukkit/entity/Player.java
+++ b/src/main/java/org/bukkit/entity/Player.java
@@ -2295,4 +2295,42 @@ public interface Player extends HumanEntity, Conversable, OfflinePlayer, PluginM
     @Override
     Spigot spigot();
     // Spigot end
+
+    // Paper start - Add gamerule player overrides
+
+    /**
+     * Gets the plugin-set immediate respawn state for this player.
+     *
+     * @return the plugin-set immediate respawn state
+     */
+    @NotNull net.kyori.adventure.util.TriState getImmediateRespawnOverride();
+
+    /**
+     * Overrides immediate respawn for this player.
+     * This causes the player to not have a respawn screen when dying, and instead are immediately respawned.
+     *
+     * @param state override state, use {@link net.kyori.adventure.util.TriState#NOT_SET} to fallback on vanilla
+     */
+    void setImmediateRespawnOverride(@NotNull net.kyori.adventure.util.TriState state);
+
+    /**
+     * Gets the plugin-set reduced debug info state for this player.
+     *
+     * @return the plugin-set reduced debug info state
+     */
+    @NotNull net.kyori.adventure.util.TriState getReducedDebugInfoOverride();
+
+    /**
+     * Overrides reduced debug info for this player.
+     * This prevents certain debug features from being rendered and
+     * removes certain debug information from the F3 menu.
+     *
+     * Currently, this prevents chunk boundaries and hitboxes from
+     * being rendered.
+     *
+     * @param state override state, use {@link net.kyori.adventure.util.TriState#NOT_SET} to fallback on vanilla
+     */
+    void setReducedDebugInfoOverride(@NotNull net.kyori.adventure.util.TriState state);
+
+    // Paper end - Add gamerule player overrides
 }
