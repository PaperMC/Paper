From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: LoaiDev <lolyakram11@gmail.com>
Date: Tue, 11 Jan 2022 18:01:33 +0200
Subject: [PATCH] Pose Api


diff --git a/src/main/java/org/bukkit/entity/Entity.java b/src/main/java/org/bukkit/entity/Entity.java
index 51ec2e4ec4239659272bba3d6ba2ad73926ebb88..9659a9dd431727b5e77197e8edb8d3154eb2fa36 100644
--- a/src/main/java/org/bukkit/entity/Entity.java
+++ b/src/main/java/org/bukkit/entity/Entity.java
@@ -667,6 +667,39 @@ public interface Entity extends Metadatable, CommandSender, Nameable, Persistent
     @NotNull
     Pose getPose();
 
+    // Paper start - Pose api
+    /**
+     * Sets the entity's current pose.
+     *
+     * <b>Note that changing the pose only has a visual effect and doesn't
+     * change other attributes such as speed, etc.</b>
+     *
+     * @param pose New {@link Pose}
+     */
+    default void setPose(@NotNull Pose pose) {
+        setPose(pose, false);
+    }
+
+    /**
+     * Sets the entity's current pose.
+     *
+     * <b>Note that changing the pose only has a visual effect and doesn't
+     * change other attributes such as speed, etc.</b>
+     *
+     * @param pose New {@link Pose}
+     * @param fixed Whether or not allow the entity to change its own pose (e.g. sleeping).<br>
+     * But plugins can still change it.
+     */
+    void setPose(@NotNull Pose pose, boolean fixed);
+
+    /**
+     * Whether or not the entity can change its own pose (e.g. sleeping).
+     *
+     * @return if entity has fixed pose
+     */
+    boolean hasFixedPose();
+    // Paper end
+
     // Spigot start
     public class Spigot extends CommandSender.Spigot {
 
