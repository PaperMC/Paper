From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Boy <sivertpaulsen2@gmail.com>
Date: Wed, 24 Jul 2024 13:41:32 +0200
Subject: [PATCH] Vault API


diff --git a/src/main/java/org/bukkit/block/Vault.java b/src/main/java/org/bukkit/block/Vault.java
index 375f41fe4aebdbf140497b5ba1e54e1c72f0cdfc..ace0652cdba2431aa4d622d2d7ef23ddc298ef12 100644
--- a/src/main/java/org/bukkit/block/Vault.java
+++ b/src/main/java/org/bukkit/block/Vault.java
@@ -1,9 +1,61 @@
 package org.bukkit.block;
 
-import org.jetbrains.annotations.ApiStatus;
+import net.kyori.adventure.key.Key;
+import org.bukkit.entity.Player;
+import org.bukkit.inventory.ItemStack;
+import org.bukkit.loot.LootTable;
+import org.jetbrains.annotations.Nullable;
+import java.util.Collection;
+import java.util.List;
+import java.util.UUID;
 
 /**
- * Represents a captured state of a trial spawner.
+ * Represents a captured state of a vault.
  */
 public interface Vault extends TileState {
+
+    // Paper start - Vault API
+    // Vault Config
+    LootTable getLootTable();
+
+    void setLootTable(LootTable lootTable);
+
+    @Nullable
+    LootTable getOverrideDisplayLootTable();
+
+    void setOverrideDisplayLootTable(@Nullable LootTable lootTable);
+
+    ItemStack getKeyItem();
+
+    void setKeyItem(ItemStack item);
+
+    double getActivationRange();
+
+    void setActivationRange(double activationRange);
+
+    double getDeactivationRange();
+
+    void setDeactivationRange(double deactivationRange);
+
+    // Vault ServerData
+    Collection<Player> getRewardedPlayers();
+
+    Collection<UUID> getRewardedPlayerUUIDs();
+
+    boolean hasRewardedPlayer(Player player);
+
+    boolean hasRewardedPlayer(UUID uniqueId);
+
+    void addToRewardedPlayers(Player player);
+
+    void addToRewardedPlayers(UUID uniqueId);
+
+    void removeFromRewardedPlayers(Player player);
+
+    void removeFromRewardedPlayers(UUID uniqueId);
+
+    List<ItemStack> getItemsToEject();
+
+    void setItemsToEject(List<ItemStack> itemsToEject);
+    // Paper end - Vault API
 }
