From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Spottedleaf <Spottedleaf@users.noreply.github.com>
Date: Thu, 31 Mar 2022 05:11:37 -0700
Subject: [PATCH] Ensure entity passenger world matches ridden entity

Bad plugins doing this would cause some obvious problems...

diff --git a/src/main/java/net/minecraft/world/entity/Entity.java b/src/main/java/net/minecraft/world/entity/Entity.java
index 90d3d2d16e989750929dda82091d2d85cf552482..bb585ae8679438f730f65d720da748a2b452b4c8 100644
--- a/src/main/java/net/minecraft/world/entity/Entity.java
+++ b/src/main/java/net/minecraft/world/entity/Entity.java
@@ -2869,6 +2869,12 @@ public abstract class Entity implements Nameable, EntityAccess, CommandSource {
     }
 
     protected void addPassenger(Entity passenger) {
+        // Paper start
+        if (passenger.level != this.level) {
+            LOGGER.error("Entity passenger world must match, cannot add " + passenger + " as passenger to " + this, new Throwable());
+            return;
+        }
+        // Paper end
         if (passenger == this) throw new IllegalArgumentException("Entities cannot become a passenger of themselves"); // Paper - issue 572
         if (passenger.getVehicle() != this) {
             throw new IllegalStateException("Use x.startRiding(y), not y.addPassenger(x)");
